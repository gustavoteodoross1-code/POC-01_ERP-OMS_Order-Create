{
  "info": {
    "name": "POC-01 ERP→OMS",
    "_postman_id": "b1f2c3d4-0000-0000-0000-000000000000",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "POST Create Sales Order in OMS",
      "request": {
        "method": "POST",
        "header": [
          { "key": "Content-Type", "value": "application/json" },
          { "key": "X-Correlation-Id", "value": "{{cid}}", "type": "text" }
        ],
        "auth": {
          "type": "basic",
          "basic": [
            { "key": "username", "value": "{{user}}", "type": "string" },
            { "key": "password", "value": "{{pass}}", "type": "string" }
          ]
        },
        "url": {
          "raw": "{{host}}/http/orders",
          "host": ["{{host}}"],
          "path": ["http", "orders"]
        },
        "body": {
          "mode": "raw",
          "raw": "{\"orderId\":\"SO-1001\",\"total\":100}"
        }
      }
    },
    {
      "name": "GET should return 405",
      "request": {
        "method": "GET",
        "auth": {
          "type": "basic",
          "basic": [
            { "key": "username", "value": "{{user}}", "type": "string" },
            { "key": "password", "value": "{{pass}}", "type": "string" }
          ]
        },
        "url": {
          "raw": "{{host}}/http/orders",
          "host": ["{{host}}"],
          "path": ["http", "orders"]
        }
      }
    }
  ],
  "variable": [
    { "key": "host", "value": "https://<seu-subaccount>.it-cpitrial06-rt.cfapps.us10-001.hana.ondemand.com" },
    { "key": "user", "value": "seu.usuario@example.com" },
    { "key": "pass", "value": "sua-senha" },
    { "key": "cid",  "value": "postman-{{$timestamp}}" }
  ]
}
